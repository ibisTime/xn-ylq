<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cdkj.ylq.dao.IStagingRuleDAO">
	<resultMap id="stagingRule_DB_Result" type="stagingRule">
		<result column="code" property="code" />
		<result column="count" property="count" />
		<result column="cycle" property="cycle" />
		<result column="rate" property="rate" />
		<result column="order_no" property="orderNo" />
		<result column="updater" property="updater" />
		<result column="update_datetime" property="updateDatetime" />
		<result column="remark" property="remark" />
		<result column="company_code" property="companyCode" />
	</resultMap>
	<sql id="where_condition">
		<trim prefix="WHERE" prefixOverrides="AND | OR">
			<if test="code != null and code != '' ">
				AND t.code = #{code}
			</if>
		</trim>
	</sql>
	<select id="select_stagingRule" parameterType="stagingRule" resultMap="stagingRule_DB_Result">
		SELECT
		t.code
		,t.count
		,t.cycle
		,t.rate
		,t.order_no
		,t.updater
		,t.update_datetime
		,t.remark
		,t.company_code
		FROM
		tjd_stage_rule t
		<include refid="where_condition" />
		<trim prefix="ORDER BY ">
			<if test="order != null and order != '' ">
				t.${order}
			</if>
		</trim>
	</select>

	<select id="select_stagingRule_count" parameterType="stagingRule"
		resultType="java.lang.Long">
		SELECT count(1) FROM tjd_stage_rule t
		<include refid="where_condition" />
	</select>

	<insert id="insert_stagingRule" parameterType="stagingRule">
		INSERT INTO
		tjd_stage_rule(
		code
		,count
		,cycle
		,rate
		,order_no
		,updater
		,update_datetime
		,remark
		,company_code
		)
		VALUES(
		#{code}
		,#{count}
		,#{cycle}
		,#{rate}
		,#{orderNo}
		,#{updater}
		,#{updateDatetime}
		,#{remark}
		,#{companyCode}
		)
	</insert>

	<update id="update_stagingRule" parameterType="stagingRule">
		UPDATE tjd_stage_rule
		SET
		count = #{count}
		,cycle = #{cycle}
		,rate = #{rate}
		,order_no = #{orderNo}
		,updater = #{updater}
		,update_datetime = #{updateDatetime}
		,remark = #{remark}
		WHERE code = #{code}
	</update>

	<delete id="delete_stagingRule" parameterType="stagingRule">
		DELETE FROM tjd_stage_rule
		WHERE code=#{code}
	</delete>

</mapper>